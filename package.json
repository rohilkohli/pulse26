{
  "name": "pulse26",
  "version": "1.0.0",
  "description": "Pulse26 — GenAI Stadium Operations & Fan Experience Platform · FIFA World Cup 2026",
  "private": true,
  "workspaces": [
    "server",
    "apps/web"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=server\" \"npm run dev --workspace=apps/web\"",
    "build": "npm run build --workspace=server && npm run build --workspace=apps/web",
    "test": "npm run test --workspace=server && npm run test --workspace=apps/web",
    "lint": "npm run lint --workspace=server && npm run lint --workspace=apps/web"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
